# ที่อยู่ของ n8n instance ของคุณ
# - สำหรับ n8n Desktop: localhost
# - สำหรับ n8n Cloud: your-subdomain.n8n.cloud
# - สำหรับ Self-hosted: โดเมนหรือ IP ของเซิร์ฟเวอร์ที่คุณติดตั้ง n8n
# (ไม่ต้องใส่ http:// หรือ https://)
AI_HOST=

# Port ของ n8n
# - สำหรับ n8n Desktop: 5678
# - สำหรับ n8n Cloud หรือ Self-hosted ที่ใช้ port มาตรฐาน (80/443): เว้นว่างไว้
AI_PORT=

# Path ของ Webhook ที่ตั้งไว้ใน n8n
# ตัวอย่าง: หาก Test URL ของ Webhook คือ https://your-n8n.com/webhook-test/1/ai-chat
# ค่าที่ต้องใส่คือ /webhook-test/1/ai-chat
AI_PATH=

# ใช้ HTTPS (true) หรือ HTTP (false) ในการเชื่อมต่อ
# - สำหรับ n8n Cloud หรือ Self-hosted ที่มี SSL: true
# - สำหรับ n8n Desktop หรือ Local instance ที่ไม่มี SSL: false
USE_HTTPS=true

# Basic Auth credentials ที่ได้จาก Webhook Node
# รูปแบบ: username:password
# ตัวอย่าง: user-123:abc-xyz-456
AI_AUTH=

# เปิด/ปิดโหมดดีบักเพื่อดู log การทำงาน (true/false)
DEBUG_MODE=false